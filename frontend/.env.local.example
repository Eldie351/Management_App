# Copiez ce fichier en ".env.local" (cp .env.local.example .env.local) puis
# ajustez si besoin.

# URL de base de l'API backend, appelée depuis le navigateur du client.
NEXT_PUBLIC_API_URL=http://localhost:3001
